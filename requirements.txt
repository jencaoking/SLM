# 问答系统模型训练脚本依赖包
# 适配中国大陆网络环境，优先使用国内镜像源安装

# 深度学习框架
torch>=1.12.0
torchvision>=0.13.0
torchaudio>=0.12.0

# 数据处理
numpy>=1.21.0
pandas>=1.3.0
scikit-learn>=1.0.0

# 文本处理和分词
jieba>=0.42.1
transformers>=4.15.0
tokenizers>=0.13.0

# 数据格式支持
pyarrow>=6.0.0  # Parquet支持
openpyxl>=3.0.0  # Excel支持
xlrd>=2.0.0     # Excel读取

# 配置文件
PyYAML>=6.0
configparser>=5.0.0

# 进度条和可视化
tqdm>=4.62.0
matplotlib>=3.5.0
seaborn>=0.11.0
tensorboard>=2.8.0

# 日志和监控
loguru>=0.6.0

# HTTP请求（用于下载数据集）
requests>=2.25.0
urllib3>=1.26.0

# 数据验证和测试
pytest>=6.2.0
pytest-cov>=3.0.0

# 代码质量
flake8>=4.0.0
black>=22.0.0

# Jupyter支持（可选）
jupyter>=1.0.0
ipykernel>=6.0.0
notebook>=6.4.0

# 自然语言处理评估
nltk>=3.7
rouge-score>=0.1.2
sacrebleu>=2.0.0

# 内存优化
psutil>=5.8.0
memory-profiler>=0.60.0

# 环境和路径处理
python-dotenv>=0.19.0
pathlib2>=2.3.6

# 数据可视化
plotly>=5.0.0
wordcloud>=1.8.2

# 多进程支持
joblib>=1.1.0
multiprocessing-logging>=0.3.0

# 类型检查（开发时使用）
mypy>=0.910
types-PyYAML>=6.0.0
types-requests>=2.25.0

# 性能分析（可选）
line-profiler>=3.0.0
py-spy>=0.3.0

# 中文分词增强（可选）
pyltp>=0.2.1
pkuseg>=0.0.25
thulac>=0.2.1

# 数据增强（可选）
nlpaug>=1.1.0
textaugment>=1.3.0

# API服务（如果需要部署API）
fastapi>=0.75.0
uvicorn>=0.17.0
pydantic>=1.9.0

# 缓存支持
diskcache>=5.4.0
redis>=4.0.0

# 分布式训练（可选）
accelerate>=0.15.0

# 模型量化（可选）
onnx>=1.12.0
onnxruntime>=1.12.0

# 数据库支持（可选）
sqlalchemy>=1.4.0
sqlite3  # Python内置

# 云存储支持（可选）
boto3>=1.20.0  # AWS S3
oss2>=2.15.0   # 阿里云OSS

# 安装说明:
# 1. 建议使用国内镜像源安装：
#    pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
#
# 2. 对于PyTorch，建议从官网安装以获得CUDA支持：
#    pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#
# 3. 如果网络环境有限制，可以考虑使用离线安装：
#    pip download -r requirements.txt -d ./packages
#    pip install --find-links ./packages -r requirements.txt --no-index
#
# 4. 最小化安装（仅核心功能）：
#    pip install torch numpy pandas jieba PyYAML tqdm scikit-learn
#
# 5. GPU环境检查：
#    python -c "import torch; print(torch.cuda.is_available())"